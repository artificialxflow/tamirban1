# نیازمندی‌های پروژه تعمیربان - نسخه بازنویسی شده

این سند شامل تمام نیازمندی‌های پروژه با اولویت‌بندی و جزئیات فنی است.

---

## 🔴 باگ‌ها و اصلاحات فوری (P0)

### 1. خطای دانلود PDF پیش‌فاکتور
**مشکل:** دانلود PDF پیش‌فاکتور خطا دارد  
**فایل‌های مرتبط:**
- `app/api/invoices/[invoiceId]/pdf/route.ts`
- `lib/utils/pdf-generator.ts`
- `components/invoices/invoice-detail-view.tsx`

**اقدامات:**
- بررسی و رفع خطاهای PDF generation
- تست در محیط production
- بهبود error handling

---

### 2. Placeholder قیمت‌ها پیش‌فرض 0
**مشکل:** Placeholder قیمت‌ها پیش‌فرض 0 است و تغییر دادن آن سخت است  
**فایل‌های مرتبط:**
- `components/invoices/invoice-create-modal.tsx`
- `components/invoices/invoice-edit-modal.tsx`

**اقدامات:**
- حذف مقدار پیش‌فرض 0 از input های قیمت
- استفاده از placeholder مناسب (مثال: "مثال: 18000000")
- بهبود UX برای وارد کردن قیمت

---

### 3. دکمه ویرایش مشتری - مشکل Back Button
**مشکل:** دکمه Back در صفحه ویرایش مشتری درست کار نمی‌کند  
**فایل‌های مرتبط:**
- `components/customers/customer-edit-modal.tsx`
- `app/dashboard/customers/page.tsx`

**اقدامات:**
- بررسی و رفع مشکل navigation
- تست Back button در تمام صفحات

---

### 4. دکمه ذخیره تغییرات ویزیت
**مشکل:** دکمه ذخیره تغییرات ویزیت درست کار نمی‌کند  
**فایل‌های مرتبط:**
- `components/visits/visit-edit-modal.tsx`
- `app/dashboard/visits/actions.ts`

**اقدامات:**
- بررسی و رفع مشکل save action
- تست ذخیره تغییرات

---

### 5. مودال‌ها در موبایل - مشکل اسکرول
**مشکل:** مودال‌ها در حالت موبایل مشکل اسکرول دارند (اسکرول خیلی زیاد)  
**فایل‌های مرتبط:**
- تمام فایل‌های `components/*/*-modal.tsx`

**اقدامات:**
- بهبود responsive design برای مودال‌ها
- محدود کردن ارتفاع مودال‌ها در موبایل
- بهبود اسکرول داخلی مودال

---

### 6. واحد قیمت‌ها
**نیازمندی:** واحد قیمت‌ها باید مشخص باشد (تومان)  
**فایل‌های مرتبط:**
- `components/invoices/invoice-create-modal.tsx`
- `components/invoices/invoice-edit-modal.tsx`
- `lib/utils/pdf-generator.ts`

**اقدامات:**
- نمایش واحد "تومان" در تمام جاهایی که قیمت نمایش داده می‌شود
- افزودن واحد به PDF پیش‌فاکتور

---

## 🟠 قابلیت‌های جدید - اولویت بالا (P1)

### 7. ویرایش نقش بازاریاب
**نیازمندی:** در موقع ویرایش بازاریاب، باید قابلیت ویرایش نقش وجود داشته باشد  
**جزئیات:** فقط مدیر کل (SUPER_ADMIN) می‌تواند نقش کاربران را تغییر دهد  
**فایل‌های مرتبط:**
- `components/marketers/marketer-edit-modal.tsx`
- `lib/services/marketers.service.ts`
- `app/dashboard/marketers/actions.ts`

**اقدامات:**
- بررسی دسترسی کاربر فعلی
- نمایش/مخفی کردن فیلد نقش بر اساس نقش کاربر
- افزودن validation برای تغییر نقش

---

### 8. نقشه کلیک برای شناسایی لوکیشن
**نیازمندی:** در هر جایی که نقشه باز می‌شود، با کلیک روی نقشه، لوکیشن شناسایی شود و روی آن برود  
**جزئیات:** 
- کلیک روی نقشه → انتخاب مختصات
- Reverse Geocoding → تبدیل مختصات به آدرس
- پر کردن خودکار فیلد آدرس

**فایل‌های مرتبط:**
- `components/visits/neshan-map.tsx`
- `components/customers/customer-create-modal.tsx`
- `components/customers/customer-edit-modal.tsx`
- `components/visits/visit-create-modal.tsx`

**اقدامات:**
- فعال کردن `interactive` mode در تمام نقشه‌ها
- پیاده‌سازی Reverse Geocoding با API نشان
- پر کردن خودکار فیلد آدرس

---

### 9. دکمه ویزیت در ثبت مشتری
**نیازمندی:** موقع ثبت مشتری، دکمه ویزیت هم باشد که مستقیماً بتواند ویزیت ثبت کند  
**فایل‌های مرتبط:**
- `components/customers/customer-create-modal.tsx`
- `components/visits/visit-create-modal.tsx`

**اقدامات:**
- افزودن دکمه "ثبت ویزیت" در مودال ثبت مشتری
- باز کردن مودال ویزیت با مشتری انتخاب شده

---

### 10. جستجو با استفاده از لوکیشن
**نیازمندی:** جستجو با استفاده از لوکیشن - کاربر با لوکیشن خودش جستجو بزند، مشتری‌های نزدیکش نمایش داده شوند  
**فایل‌های مرتبط:**
- `lib/services/customers.service.ts`
- `app/api/customers/route.ts`
- `components/customers/customer-filters.tsx`

**اقدامات:**
- افزودن فیلتر location-based search
- استفاده از MongoDB geospatial queries
- نمایش فاصله از کاربر

---

### 11. نوتیفیکیشن ویزیت‌ها
**نیازمندی:** ویزیت‌ها باید نوتیفیکیشن داشته باشند در زمان ویزیت  
**فایل‌های مرتبط:**
- `lib/services/visits.service.ts`
- نیاز به سرویس نوتیفیکیشن (Push Notifications / SMS)

**اقدامات:**
- پیاده‌سازی سیستم نوتیفیکیشن
- ارسال نوتیفیکیشن قبل از زمان ویزیت (مثلاً 1 ساعت قبل)
- مدیریت نوتیفیکیشن‌های برنامه‌ریزی شده

---

### 12. برچسب‌های از پیش تعریف شده برای مشتری
**نیازمندی:** برای مشتری برچسب‌های از پیش تامین شده: مکانیک، برق کار، تنظیم موتور، جلوبندی ساز، آپاراتی، تعویض روغن، کارواش، صافکار، کلیدساز  
**فایل‌های مرتبط:**
- `lib/types/domain.ts` - افزودن constant برای برچسب‌ها
- `components/customers/customer-create-modal.tsx`
- `components/customers/customer-edit-modal.tsx`
- `lib/services/customers.service.ts` - افزودن فیلتر بر اساس برچسب

**اقدامات:**
- ایجاد لیست ثابت برچسب‌ها
- تبدیل input text به select/multi-select
- افزودن قابلیت جستجو بر اساس برچسب

---

### 13. چند شماره تلفن برای مشتری
**نیازمندی:** قابلیت افزودن چند شماره برای مشتری وجود داشته باشد  
**فایل‌های مرتبط:**
- `lib/types/domain.ts` - تغییر `ContactInfo` به `phone: string[]`
- `components/customers/customer-create-modal.tsx`
- `components/customers/customer-edit-modal.tsx`
- `lib/services/customers.service.ts`

**اقدامات:**
- تغییر مدل داده برای پشتیبانی از چند شماره
- افزودن UI برای افزودن/حذف شماره‌ها
- Migration داده‌های موجود

---

### 14. تاریخچه ارتباط با مشتری
**نیازمندی:** تاریخچه ارتباط با مشتری‌ها ثبت شود و قابل مشاهده باشد  
**فایل‌های مرتبط:**
- ایجاد مدل جدید `CustomerInteraction` در `lib/types/domain.ts`
- ایجاد repository برای interactions
- ایجاد service برای مدیریت interactions
- ایجاد UI برای نمایش تاریخچه

**اقدامات:**
- طراحی مدل داده برای تاریخچه ارتباط
- ثبت خودکار ارتباطات (تماس، ویزیت، پیامک)
- ایجاد صفحه نمایش تاریخچه

---

### 15. تنظیم ویزیت و تاریخ جلسه بعدی
**نیازمندی:** برای بازاریاب تنظیم ویزیت‌ها و تاریخ جلسه بعدی قابل تنظیم باشد، با نوتیفیکیشن  
**فایل‌های مرتبط:**
- `components/visits/visit-create-modal.tsx`
- `components/visits/visit-edit-modal.tsx`
- `lib/services/visits.service.ts`

**اقدامات:**
- افزودن فیلد "تاریخ جلسه بعدی" به ویزیت
- برنامه‌ریزی نوتیفیکیشن برای جلسه بعدی
- نمایش جلسات آینده در داشبورد

---

### 16. نوع ویزیت (حضوری/تلفنی)
**نیازمندی:** ویزیت‌ها به صورت حضوری یا تلفنی مشخص شوند  
**فایل‌های مرتبط:**
- `lib/types/domain.ts` - افزودن `visitType: "IN_PERSON" | "PHONE"`
- `components/visits/visit-create-modal.tsx`
- `components/visits/visit-edit-modal.tsx`
- `lib/services/visits.service.ts`

**اقدامات:**
- افزودن فیلد نوع ویزیت به مدل داده
- افزودن select در فرم‌های ویزیت
- فیلتر بر اساس نوع ویزیت

---

### 17. نمایش پیش‌فاکتور در ویزیت
**نیازمندی:** پیش‌فاکتور ثبت شده در ویزیت قابل دیدن باشد  
**فایل‌های مرتبط:**
- `components/visits/visit-detail-modal.tsx`
- `lib/services/visits.service.ts`
- `lib/services/invoices.service.ts`

**اقدامات:**
- ارتباط ویزیت با پیش‌فاکتور
- نمایش لیست پیش‌فاکتورهای مرتبط با ویزیت
- لینک به جزئیات پیش‌فاکتور

---

### 18. محدودیت دانلود شماره‌ها برای بازاریاب
**نیازمندی:** هر بازاریابی نتواند همه شماره‌ها را برای خودش ذخیره کند. در استفاده محدودیت نداشته باشد، ولی نتواند سوء استفاده کند و کامل دانلود کند  
**جزئیات:**
- حذف دکمه Export/Download برای بازاریاب‌ها
- محدود کردن API endpoint `/api/customers` برای بازاریاب‌ها (فقط مشتری‌های اختصاص یافته)
- افزودن Rate Limiting
- لاگ کردن تمام دسترسی‌ها

**فایل‌های مرتبط:**
- `app/api/customers/route.ts` - افزودن permission check
- `lib/middleware/rbac.ts` - بررسی دسترسی
- `lib/services/customers.service.ts` - فیلتر بر اساس marketerId

**اقدامات:**
- بررسی نقش کاربر در API
- محدود کردن نتایج برای بازاریاب‌ها
- حذف دکمه Export از UI برای بازاریاب‌ها
- افزودن Rate Limiting
- لاگ دسترسی‌ها

---

## 🟡 قابلیت‌های جدید - اولویت متوسط (P2)

### 19. تبدیل پیش‌فاکتور به فاکتور
**نیازمندی:** پیش‌فاکتور قابل تبدیل به فاکتور باشد. مبلغ چک، تاریخ چک، صاحب چک و ... وارد شود. حالت‌های در انتظار، تسویه و ... مشخص شود  
**فایل‌های مرتبط:**
- `lib/types/domain.ts` - افزودن مدل `Invoice` با فیلدهای چک
- `components/invoices/invoice-status-change-modal.tsx`
- `lib/services/invoices.service.ts`

**اقدامات:**
- طراحی مدل داده برای اطلاعات چک
- افزودن فیلدهای پرداخت به Invoice
- ایجاد UI برای ثبت اطلاعات چک
- مدیریت وضعیت‌های پرداخت

---

### 20. مدیریت محصولات
**نیازمندی:** بستر تعریف محصولات و قیمت‌گذاری. در قسمت ایجاد پیش‌فاکتور لیست محصولات به صورت کشویی نمایش داده شود و قابل انتخاب باشد  
**فایل‌های مرتبط:**
- `lib/types/domain.ts` - مدل `Product` موجود است
- ایجاد `app/dashboard/products/page.tsx`
- ایجاد `components/products/` - کامپوننت‌های مدیریت محصولات
- ایجاد `app/api/products/route.ts`
- ایجاد `lib/services/products.service.ts`
- `components/invoices/invoice-create-modal.tsx` - استفاده از محصولات

**اقدامات:**
- ایجاد CRUD کامل برای محصولات
- ایجاد صفحه مدیریت محصولات
- افزودن select محصولات در فرم پیش‌فاکتور
- قیمت‌گذاری خودکار از محصولات

---

### 21. مدیریت تسک
**نیازمندی:** مدیر بتواند برای بازاریاب و مدیر و نقش‌های دیگر تسک تعیین کند با نوتیفیکیشن  
**فایل‌های مرتبط:**
- ایجاد مدل `Task` در `lib/types/domain.ts`
- ایجاد `app/dashboard/tasks/page.tsx`
- ایجاد `components/tasks/` - کامپوننت‌های مدیریت تسک
- ایجاد `app/api/tasks/route.ts`
- ایجاد `lib/services/tasks.service.ts`

**اقدامات:**
- طراحی مدل داده برای تسک
- ایجاد CRUD برای تسک
- اختصاص تسک به کاربران
- نوتیفیکیشن برای تسک‌ها

---

### 22. گزارش‌های اختصاصی برای مدیر
**نیازمندی:** مدیر اصلی گزارش‌های اختصاصی برای تک تک اعضا بگیرد و گزارش کامل دسترسی داشته باشد (مثل فاکتور و ...)  
**فایل‌های مرتبط:**
- `app/dashboard/reports/page.tsx`
- `lib/services/reports.service.ts`
- `app/api/reports/route.ts`

**اقدامات:**
- ایجاد سیستم گزارش‌گیری
- گزارش برای هر بازاریاب
- گزارش فاکتورها
- Export به Excel/PDF

---

## 🔵 بهبودهای UX/UI (P3)

### 23. بهبود نقش‌ها و دسترسی‌ها
**نیازمندی:** نقش‌ها: مشتری – بازاریاب – مدیر، هر کدام با قابلیت‌های ممکن قابل استفاده باشد  
**فایل‌های مرتبط:**
- `lib/permissions/role-permissions.ts`
- `lib/middleware/rbac.ts`

**اقدامات:**
- بررسی و بهبود دسترسی‌های هر نقش
- تست دسترسی‌ها
- مستندسازی دسترسی‌ها

---

## 📱 اپلیکیشن مشتری (Flutter) - فاز بعدی

### 24. ثبت اطلاعات مشتری در اپ
**نیازمندی:** اپ مشتری پس از ورود اطلاعات خودش را ثبت کند  
**فایل‌های مرتبط:**
- پروژه Flutter (خارج از این repository)

**اقدامات:**
- طراحی UI ثبت اطلاعات
- API برای ثبت اطلاعات مشتری
- Validation و error handling

---

### 25. ارتباط با ما
**نیازمندی:** ارتباط با ما برای خرابی - شماره تماس برای تماس تلفنی یا پیام، پیام داخلی  
**جزئیات:**
- شماره تماس برای تماس مستقیم
- دکمه تماس
- فرم پیام داخلی
- لینک به شبکه‌های اجتماعی (تلگرام، واتساپ)

**فایل‌های مرتبط:**
- پروژه Flutter
- `app/api/contact/route.ts` - برای پیام داخلی

**اقدامات:**
- طراحی صفحه ارتباط با ما
- پیاده‌سازی تماس تلفنی
- فرم پیام داخلی
- لینک به شبکه‌های اجتماعی

---

### 26. نمایش لیست محصولات در اپ
**نیازمندی:** لیست محصولات را بتواند ببیند  
**فایل‌های مرتبط:**
- پروژه Flutter
- `app/api/products/route.ts` - باید public endpoint باشد

**اقدامات:**
- ایجاد API public برای محصولات
- طراحی UI لیست محصولات
- فیلتر و جستجو

---

### 27. استوری‌های مدیر
**نیازمندی:** مدیر بتواند استوری بگذارد که کاربران اپ استوری‌ها را ببینند  
**فایل‌های مرتبط:**
- ایجاد مدل `Story` در `lib/types/domain.ts`
- `app/dashboard/stories/page.tsx` - مدیریت استوری‌ها
- `app/api/stories/route.ts`
- پروژه Flutter - نمایش استوری‌ها

**اقدامات:**
- طراحی مدل داده برای استوری
- ایجاد CRUD برای استوری
- API برای دریافت استوری‌ها
- UI در Flutter برای نمایش استوری

---

### 28. لینک شبکه‌های اجتماعی
**نیازمندی:** لینک شبکه‌های اجتماعی باشد  
**فایل‌های مرتبط:**
- پروژه Flutter
- `app/api/settings/social-links/route.ts` - برای مدیریت لینک‌ها

**اقدامات:**
- ایجاد API برای مدیریت لینک‌های اجتماعی
- UI در Flutter برای نمایش لینک‌ها
- باز کردن لینک‌ها در اپلیکیشن خارجی

---

## یادداشت‌ها

- تمام نیازمندی‌ها با اولویت‌بندی مشخص شده‌اند
- فایل‌های مرتبط برای هر نیازمندی ذکر شده است
- نیازمندی‌های مبهم با جزئیات بیشتر توضیح داده شده‌اند
- نیازمندی‌های مربوط به Flutter در بخش جداگانه قرار گرفته‌اند
